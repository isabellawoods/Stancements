package melonystudios.stancements.misc.recording;

import net.minecraft.resources.ResourceLocation;

// list of commonly used music tracks, so i don't have to constantly write these ~isa 23-07-26
public class Tracks {
    public static final Track C418_ALPHA = new Track(ResourceLocation.withDefaultNamespace("game/end/alpha"), true);
}
